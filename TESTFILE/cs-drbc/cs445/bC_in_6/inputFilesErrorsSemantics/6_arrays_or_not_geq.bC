int main()
{
    int x:1;
    char y:'b';
    bool z:true;

    int xx[1];
    char yy[1];
    bool zz[1];

    xx[0] = x;
    yy[0] = y;
    zz[0] = z;

    x >= xx;
    x >= yy;
    x >= zz;

    y >= xx;
    y >= yy;
    y >= zz;

    z >= xx;
    z >= yy;
    z >= zz;

    xx >= x;
    xx >= y;
    xx >= z;

    yy >= x;
    yy >= y;
    yy >= z;

    zz >= x;
    zz >= y;
    zz >= z;

    return x;
}
