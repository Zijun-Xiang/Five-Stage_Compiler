#DRBC This is my first program
#DRBC The course name is Compiler Design

char zev[10]:"corgis";
int x:42, y:666;

junk(){
   return;
}

int ant(int bat, cat[]; bool dog, elk; int fox; char gnu){
int goat, hog[100];
char junks : 'x' ; 
junk();
gnu = 'W';
goat = hog[2] = 3**cat; // hog is 3 times the size of array passed to cat

   if dog and elk or bat > cat[3] then dog = not dog;
   else fox++;

   if bat <= fox then {
   while dog do {

   static int hog; // hog in new scope
hog = fox;
dog = fred(fox++, cat)>666;

   if (hog>bat) then {
      break;
   }else if (fox !=0) then {
       fox += 7;
   }
   }
   }
for i=1 to 10 by 3 do { // i is an int local to the loop

   if x==1 then cat[i] = bat;

   else if (x==1)then {
       cat[i] = bat%17;
   }
   else if (x>= -3)then {
       cat[i] = 78;;
   }else {x++;
   }
    if (x>333)then {
    break; 
    }else{
        x--;
     }
   }
   x = ? 10;
   x = 5 - 3; 
return (fox+bat*cat[bat])/-fox;
}
// note that functions are defined using a statement
//int max(int a, b) if a>b then return a; else return b;


//int pointless_wapper(int num) output(num);

//int pointless_math(int num1,num2) {
  //  output(num1 * num2);
   // output(num1 / num2);
  //  output(num1 + num2);
   // output(num1 - num2);
   // return;
//}

int main(){

    int x:5;
    int y, z;
    static int abc:123;
    bool k;

    for m=0 to 6
    do{
    if x < y
    then y += 1;
      break;
   }
//DRBC Rule 19 & 20 (66, 72 & 73)
   for m = 0 to 100 do {// 19a
      //20d, 19b, 20b, 20c
      for n = 0 to 100 by 2 do
         if (true == true) then {y += 2;}
         else while (true == false) do
             if (false != false) then y = -100;
   }
 
   // MIN and MAX
   x = 10 :<: 5;
   y = 10 :>: 5;
   x += 6;
   x -= 6;
   x *= 6;
   x /= 6;

//   for m=0 to 100 do{ //19a
  //   for n=0 to 100 by 2
    //      if(true == true) then {happy +=20;}
      //    else{
        //    while (true == false)do
          //  if(false != false) then happy -= 100;
         // }
  // }

 // if x < y or x == y
   //then y-= 1;
  // else if x>y and x == y
    //  then y *=1;

}

