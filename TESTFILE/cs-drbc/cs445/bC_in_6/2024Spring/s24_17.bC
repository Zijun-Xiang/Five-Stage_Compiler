#DRBC My first program  
#DRBC precompile list  

int function1(int a, b)
{
    return a+b;
}

function2()
{
    return;
}


int pointlesswapper(int num) output(num);

int pointlessmath(int num1, num2){
    output(num1 * num2);
    output(num1 / num2);
    output(num1 + num2);
    output(num1 - num2);
}

int pointlesscheck(int num1, num2; char ch1[], diff[]){
    char bad[10];
    bad[0]='a';
}

int max(int a, b) if a>b then return a; else return b;

int acc, abb[4];

cpp();

int main()
{
  
    int x:5;
    int m[0]:2;
    int y, z;
    static int abc:123;
    bool bbb;
    int arr[10];
    char zev[10]:"apple";
    int i: x();
    int j: m(1,2); 

    while(happy == 0) do {
        if(godvibes > 0) then {
            happy += 1;
        }
        else {
            happy -=1;
        }
    }

    for m=0 to 6
    do{
        if x < y
        then y += 1;
            break;
    }

    if x < y or x == y
    then y -= 1;
    else if x > y and x == y
        then y *= 1;

    z = x+y;
    z = x-y;
    z = x*y;
    z = x%y;
    z = x/y;
    z--;
    z++;
    z /= x;
    z = ?10;
    
    //unaryop
    z = -2;
    z = *2;

    //rule34,35
    y = 3 :>: 5;
    y = 4 :<: 10;



    //rule19,20
    for m=0 to 20 do{
	for n=0 to 10 by 2 do
	    if(true == true) then {happy += 100;}
	    else
		while (true == false) do
		if(false != false) then {happy -= 100;}
    } 
    
    //rule 33
    if x <= y or x >= y
    then y -= 1;
    else y/x;

  
    if x < y or x == y
    then y -= 1;
    else if not (x == y)
        then y *= 1;
 


    
}
