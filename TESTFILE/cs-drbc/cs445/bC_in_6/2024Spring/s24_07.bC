#DRBC HW01
char zev[10]:"corgis";
int x:42, y:666;
int ant(int bat, cat[]; bool  dog,  elk; int fox; char  gnu) {
        int goat, hog[100];
        gnu = 'W';
        goat = hog[2]   = 3**cat;

        if dog and elk or bat > cat[3] then dog = not dog;
        else fox++;

        if bat <= fox then {
                while dog do {
                        static int hog;
                        hog = fox;
                        dog = fred(fox++,  cat)>666;
                        if hog > bat then break;
                        else if fox !=0 then  fox  += 7;
                }
        }

        for i=1 to 10 by 3 do {
                if x==1 then cat[i] = bat;
                else if(x==2) then cat[i] = bat%17;
                else if(x==3) then cat[i] = 78;
                else x++;
        }

        while x<=333 do x++;
        return (fox+bat*cat[bat])/-fox;
}

int max(int a, b) {
        if a > b then return a;
        else return b;
}

int min(int a; int b){
        if a < b then return a;
        else return b;
}

char intToSign(int a){
        if a > 0 then return '1';
        else if a == 0 then return '0';
        else return '-';
}

int give5(){
        return 5;
}

norettypefunc(char a){
        char b:a;
}

int main(){
        int o:1,p:5;
        int count:0;

        for count=o to p do{
                count++;
        }

        if o>=p then o=p;
        else while o<p do{
                o++;
        }
        o*=2;
        o+=2;
        o/=2;
        p--;
        p-=1;;

        count=?2;
        count=p-1;

        count=give5();

        count=max(o,p);
        count=min(o,p);

        p=5:>:4;
        o=3:<:4;

           //DRBC Rule 19 & 20 (66, 72 & 73)
   for m = 0 to 100 do {// 19a
      //20d, 19b, 20b, 20c
      for n = 0 to 100 by 2 do
         if (true == true) then {y += 2;}
         else while (true == false) do
             if (false != false) then y = -100;
}
        return;
}

